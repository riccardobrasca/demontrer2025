{
  "name": "demonter2025 dev container",

  "build": {
    "dockerfile": "Dockerfile"
  },

  "onCreateCommand": "echo \"Downloading everything\" && lake exe cache get && lake build Library && echo \"Finished setup! Open a file using the Explorer in the top-left of your screen.\"",

  "customizations": {
    "vscode" : {
      "extensions" : [ "leanprover.lean4" ]
    }
  }
}
